<%= form_with(model: food_service) do |form| %>
  <% if food_service.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(food_service.errors.count, "error") %> prohibited this food_service from being saved:</h2>

      <ul>
        <% food_service.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :contact_number %>
    <%= form.text_field :contact_number %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status %>
  </div>

  <div class="field">
    <%= form.label :time_interval %>
    <%= form.text_field :time_interval %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
