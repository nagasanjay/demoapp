<%= form_with(model: food) do |form| %>
  <% if food.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(food.errors.count, "error") %> prohibited this food from being saved:</h2>

      <ul>
        <% food.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :cost %>
    <%= form.text_field :cost %>
  </div>

  <div class="field">
    <%= form.label :units %>
    <%= form.number_field :units %>
  </div>

  <div class="field">
    <%= form.label :includes %>
    <%= form.text_field :includes %>
  </div>

  <div class="field">
    <%= form.label :available_count %>
    <%= form.number_field :available_count %>
  </div>

  <div class="field">
    <%= form.label :food_service_id %>
    <%= form.text_field :food_service_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
